[phases.setup]
nixPkgs = ['nodejs', 'yarn','discord']

[phases.install]
cmds = ['npm i tar','npm install -g npm ','npm install --omit=dev','npm ci']

[phases.build]
cmds = ["echo building!"]
cmd = ['npm run build']


